# Constantes do projeto. Fonte para a substituicao de ${BUCKET} nas queries.
#
# Formato deliberadamente simples (CHAVE=valor, sem aspas, sem expansao) para ser
# lido tanto por `source config.env` em shell quanto por parsing trivial em Python.
#
# Preencha com os valores do seu ambiente antes da primeira execucao.

PROJECT_ID=meu-projeto-gcp
BUCKET=meu-projeto-gcp-landing
LOCATION=US
